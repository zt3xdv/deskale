# Deskale (Beta test)
VNC server using the LXDE desktop environment installer for linux.

## Installation

**Download and execute the install script:**

```bash
curl -O https://raw.githubusercontent.com/zt3xdv/deskale/main/install.sh && chmod +x install.sh && sh ./install.sh
```

Now restart your server and its done!
